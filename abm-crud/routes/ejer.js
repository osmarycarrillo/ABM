//importamos el modulo fs
const fs = require ('fs') // se usa para leer o escribir archivos de la pc

fs.writeFileSync('datos.txt', 'hola mundo') // lo primero es el nombre del  archivo y lo segundo lo que voy a guardar
